🐾 猫咪异常行为监测项目 (Cat-Health-Monitor) - 开发日志
📅 2026年3月10日：数据结构化与时序逻辑构建
🎯 今日目标
1. 完成原始视频帧的清洗与分类。
2. 建立视频片段与离散图片之间的时序逻辑关联。
3. 解决不同异常行为（癫痫、呕吐）时长不一的数据对齐预想。
✅ 今日成果
• 数据集初步落成：完成了约 1900 张图片的命名标准化，格式为 {视频ID}_frame_{序号}.jpg。
• 构建时序索引表：编写并运行 Python 脚本，自动扫描数据集并生成了 metadata.csv。
• 数据规模：涵盖了从几帧（极短瞬时动作）到 227 帧（持续发病过程）的不同动作片段。
• 逻辑验证：通过 CSV 索引成功锁定了每个动作的起始与终点，实现了从“散图”到“动作片段”的逻辑封装。
• 技术决策：
• 针对变长序列问题，经过探讨，初步确定采用 “稀疏采样 (Sparse Sampling)” 策略，将所有片段统一至固定帧数（如 16 帧）。
• 确立了“主旋律”开发路线：标准图片时序识别为主，猫咪骨架关键点 (Pose Estimation) 为辅的创新思路。
🛠️ 技术思考 (Self-Reflection)
在处理 epilepsy_002 系列数据时发现，同一次发病会被切分成多个极短片段（如 6-10 帧）。这反映了动作的爆发性。后续在模型训练时，需要重点考虑如何通过“时间窗口”来捕捉这些瞬时高频特征。
📅 明日计划 (Next Steps)
1. 构建标准化 Data Loader：编写代码实现 16 帧稀疏采样，生成模型可读取的 .npy 训练数据块。
2. 骨架提取测试：尝试运行 Mediapipe 提取第一批猫咪关节坐标，验证“骨架创新路线”的可行性。
3. 补充负样本：开始收集并标注“Normal（正常状态）”的视频帧，以平衡数据集类别。

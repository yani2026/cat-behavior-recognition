# 🐱 猫咪行为识别系统 (Cat-Behavior-Recognition)

> **项目状态**：已打通 GPU 加速链路，完成首轮 CNN-LSTM 模型训练。

---

## 📅 开发日志：2026-03-14
### 🚀 今日进度：从“环境荒漠”到“显卡点火”

#### 1. 核心突破：GPU 计算环境重构
成功解决了 PyTorch 无法调用 RTX 4060 的顽疾。
* **环境修复**：卸载了错误的 CPU 版 Torch，重新部署了 `torch-2.5.1+cu121`。
* **硬件唤醒**：`torch.cuda.is_available()` 现已正式返回 `True`。
* **依赖对齐**：完整安装了 `OpenCV`, `Pandas`, `Scikit-learn` 等全套数据处理库。

#### 2. 坑位记录：权重缓存清理
* **故障现象**：模型加载时触发 `invalid hash value` 校验报错。
* **原因分析**：网络波动导致预训练权重文件下载残缺。
* **解决方案**：定位并物理删除 `~/.cache/torch/hub` 路径下的坏文件，重下后校验通过。

#### 3. 实验数据复盘 (Epoch 1-20)
| 指标 | 表现 | 备注 |
| :--- | :--- | :--- |
| **训练硬件** | RTX 4060 Laptop | 35s / Epoch |
| **验证准确率** | 100% | 疑似存在严重过拟合 |
| **Loss 曲线** | 快速收敛 | 需警惕数据同质化 |

> [!CAUTION]
> **风险预警**：目前 191 组样本主要由滑动窗口切分产生，模型可能在“背诵”背景而非学习“呕吐”动作。

---

### 🛠️ 下一步路线图 (Roadmap)
- [ ] **数据增强**：引入 `RandomHorizontalFlip` 和色彩抖动，打破像素依赖。
- [ ] **采样优化**：增大窗口滑动步长 (Stride)，降低样本重叠率。
- [ ] **实测验证**：开发推理脚本，进行非训练集视频的泛化测试。

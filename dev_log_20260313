📝 猫咪行为识别项目 - 开发日志 (2026-03-13)

1. 今日开发内容概述
今天完成了从"原始数据清洗"到"深度学习数据流水线（Data Pipeline）"构建的最核心工作。
数据增强：实现了基于“滑动窗口”算法的样本扩充，将原始视频片段切分为 191 组（每组 16 帧）标准化序列.
数据集划分：采用"分层采样（Stratified Sampling）"技术，将数据按 8:2 划分为训练集和测试集，确保了各类别（癫痫、呕吐、正常）在两组数据中的比例一致。
数据流水线构建：完成了 PyTorch 格式的 `Dataset` 类和 `DataLoader` 类编写，实现了图片的自动缩放、归一化及张量（Tensor）转化。

2. 解决的关键问题
攻克了几个可能导致项目停滞的“深坑”：
路径与命名逻辑错位：解决了 CSV 记录与硬盘实际文件夹名（如 `seizure` vs `epilepsy_selected`）不匹配的问题。
动态文件名适配：修正了不同类别视频前缀不统一（如 `normal4_...` vs `epilepsy1_...`）的报错，通过动态字符串格式化实现了自动化匹配。
Python 语法纠错：修正了 `elif` 逻辑判断中的语法错误，确保了代码的健壮性。
张量维度验证：通过阶段化测试，最终确认了模型输入维度为标准的 `[Batch, Frames, Channel, Height, Width]`。

3. 当前项目进度评估
进度条：55% 左右。
状态：数据基建已全部封顶，模型“传送带”已试运行成功。
  
4. 明日开发方向（炼丹计划）
明天的目标是让 AI 产生“意识”，开始识别动作：
模型搭建：构建 "CNN + LSTM" 串联架构。利用 MobileNetV2 提取视觉特征，利用 LSTM 提取时间序列上的动作特征。
解决不平衡问题：针对“呕吐”类样本较少的问题，引入 `WeightedLoss`（加权损失函数），防止模型产生预测偏好。
模型训练与验证：正式启动训练循环，观察 Loss 曲线下降情况，并在测试集上评估最终的识别准确率。


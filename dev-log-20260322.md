# 🐱 猫咪行为识别系统 (Cat-Behavior-Recognition)
## 1.实验迭代历程
完成从模型对比到任务转换的5组核心实验
| 实验编号 | 核心架构 | 关键变量 | 实验目的 |
| :--- | :--- | :--- | :---|
| **train001** | MobileNetV2 + LSTM | 基础 CNN-LSTM | 建立轻量化基准模型 |
| **train002** | MobileNetV2 + LSTM + Attention | 引入注意力机制 | 验证时序加权对动作识别的影响 |
| **train003** | ResNet-50 + LSTM + Attention | 强化骨干网络 | 提升空间特征提取的深度与精度 |
| **train004** | ResNet-50 + LSTM + Attention | 超参数微调 | 压制过拟合，探索 RGB 模态的性能上限 |
| **train101** | ResNet-50 + LSTM + Attention | 二分类任务 | 简化任务难度，验证模型对异常信号的判别力 |

## 2.核心实验分析：train101复盘
尽管二分类降低了分类层级，但实验结果揭示了基于像素（RGB）模态的底层缺陷：

•特征混叠 (Feature Aliasing)：在 RGB 时序识别中，相机的平移抖动（手持拍摄产生的噪声）与猫咪的病理动作（抽搐、呕吐）在特征空间内高度重叠，导致模型无法有效解耦。

•虚警率（False Positive Rate）失控：模型错误地将所有“像素位移”特征贴上“异常”标签，导致 Normal 样本的误报率接近 95%。

• 小样本下的模态局限：单纯依靠图像序列在极小样本集（约 75 个切片）下，无法对细微的病理动作完成鲁棒建模。

## 3.后续思路
基于今日实验教训，项目重心将由 RGB 像素驱动 转向 Skeleton（骨架）几何驱动：

• 方案调整：由“看图识猫”转向“看点识影”。

• 核心优势：
1. 物理降噪：通过坐标中心化（如以颈部为原点）直接对冲相机位移。
2. 数据降维：将复杂的图像流转化为 17 个关键点的坐标流，降低模型对样本量的依赖。
3. 产品化潜力：骨架数据不涉及隐私像素，且对光照、背景具备天然的鲁棒性。
